// All URLs related to Comments

const express = require("express"); // For routing we need Express module object

const router = express.Router(); // Router() is a method under Express framework

module.exports =  router;  